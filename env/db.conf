[variables]
db.name:
    dev = assmat
    prod = <external>
db.host:
    dev = localhost
    prod = <external>
db.user:
    dev = root
    prod = <external>
db.password:
    dev = passwd
    prod = <external>