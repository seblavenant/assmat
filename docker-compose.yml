web:
    image: s3b/apache-php
    ports:
        - 80:80
    volumes:
        - .:/var/www
        - .docker/confs/apache/:/etc/apache2/sites-enabled/
    links:
        - database
        - mailer
database:
    container_name: database
    image: mysql
    environment:
        - MYSQL_ROOT_PASSWORD=passwd
        - MYSQL_DATABASE=assmat
        - MYSQL_USER=root
        - MYSQL_PASSWORD=passwd

phpmyadmin:
    image: corbinu/docker-phpmyadmin
    ports :
        - 81:80
    environment:
        - MYSQL_USERNAME=root
        - MYSQL_PASSWORD=passwd
    links:
        - database:mysql

mailer:
    container_name: mailer
    image: schickling/mailcatcher
    ports:
        - 1080:1080
